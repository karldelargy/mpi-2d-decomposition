MATHOBJS = MyMath.o TestMyMath.o
TESTBIN = TestMyMath
CC = g++

LIBS = -lcunit -lm

testbin: ${MATHOBJS}
	${CC} -o ${TESTBIN} ${MATHOBJS} ${LIBS}

%.c: %.h

%.cpp: %.h

%.o: %.c
	${CC} -c -o ${<:.c=.o} $< 

clean:
	rm -rf ${MATHOBJS}
	rm -rf ${TESTBIN}
